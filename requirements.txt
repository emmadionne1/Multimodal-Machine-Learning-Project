torch
transformers
accelerate
huggingface_hub
datasets
evaluate
wandb
numpy
pillow
matplotlib

# Notes:
# - config.py is a local project file, so it does not belong in requirements.txt
# - If your cluster needs CUDA-specific PyTorch wheels, install torch separately
#   following the PyTorch install selector for your CUDA version
